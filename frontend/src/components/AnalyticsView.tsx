import type { ReactElement } from "react";

/** Placeholder view for the /analytics route. */
const AnalyticsView = (): ReactElement => {
  return <p className="tagline text-center">Analytics coming soon.</p>;
};

export { AnalyticsView };
